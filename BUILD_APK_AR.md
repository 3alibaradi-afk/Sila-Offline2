# بناء APK تلقائيًا عبر GitHub Actions

بعد رفع المشروع إلى GitHub، يبدأ البناء تلقائيًا عند كل push إلى فرع `main`.

النتيجة المتوقعة:

- Workflow: `Build Sila Android APK`
- Artifact: `Sila-Offline-MVP1-APK`
- الملف داخل الـArtifact: `app-debug.apk`

يمكن أيضًا تشغيل البناء يدويًا من تبويب Actions عبر `Run workflow`.
